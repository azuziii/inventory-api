import { NormalizeOrderItemsPipe } from './normalize-order-items.pipe';

describe('NormalizeOrderItemsPipe', () => {
  it('should be defined', () => {
    expect(new NormalizeOrderItemsPipe()).toBeDefined();
  });
});
